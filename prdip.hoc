{load_file("prcellstate.hoc")}
proc prdip() {local x  localobj f
	if (t == 102.8) {
		f = new File()
		f.wopen("temp.dat")
		forall if (ismembrane("dipole")) for (x,0) {
f.printf("%s %g %g\n", secname(), x, Q_dipole(x))
		}
		f.close
	}
}
